'use strict';
const DESIGN=document.body.dataset.design||'II';
function journalTabs(){return `<div class="filter-chips">${[['signals','Signal record'],['observations','Stage changes']].map(([k,v])=>`<button class="chip ${journalMode===k?'active':''}" data-journal="${k}" aria-pressed="${journalMode===k}">${v}</button>`).join('')}</div>`;}
function signalJournal(){const all=D.scorecard||[],start=(signalPage-1)*20,rows=all.slice(start,start+20);return `${head('Every signal has a history.','The original alert, its recorded levels, and what happened next.','JOURNAL / SIGNAL RECORD')}${journalTabs()}<div class="callout">Archived marks include open observations. Current R is not a realized trade return. Conviction here is the value recorded at the alert.</div><section class="panel"><div class="table-wrap"><table><thead><tr><th>OBSERVED</th><th>COMPANY</th><th>SIGNAL TYPE</th><th>CONVICTION AT ALERT</th><th>ENTRY / STOP</th><th>CURRENT R</th><th>BEST R</th><th>STATUS</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${esc(r.d)}</td><td>${stockButton(r.sym)}</td><td>${esc(r.kind)}</td><td class="num">${number(r.conv,1)}</td><td class="num">${money(r.entry)}<small>${money(r.stop)}</small></td><td class="num">${signed(r.r,2)}</td><td class="num">${signed(r.maxr,2)}</td><td>${esc(r.status)}</td></tr>`).join('')}</tbody></table></div><div class="pagination"><span>${all.length?start+1:0}–${Math.min(start+20,all.length)} of ${all.length} recorded signals</span><div><button data-signalpage="-1" ${signalPage===1?'disabled':''}>← Previous signals</button><button data-signalpage="1" ${start+20>=all.length?'disabled':''}>Next signals →</button></div></div></section>`;}
function industryReview(){return `<details class="panel disclosure"><summary>Cross-reference by NSE industry</summary><p class="small-note">Saved industry aggregates · average RS percentile · reference line at 50 · counts refer to the source’s saved cohort</p><div class="industry-grid">${D.heat.map(r=>`<div><h3>${esc(r.ind)}</h3><div class="dim-bar"><span style="width:${r.rs}%"></span></div><p>${number(r.rs)} average RS · ${r.n} companies</p></div>`).join('')}</div></details>`;}
function cohortReview(){return `<details class="panel disclosure"><summary>Inspect the archived cohort diagnostics</summary><p class="small-note">Descriptive, mixed-age observations with open positions. These aggregates do not establish score calibration or causal predictive value.</p>${(D.forensics.sections||[]).map(s=>`<h3 style="margin:24px 0 12px;font-size:14px">${esc(s.title)}</h3><div class="table-wrap"><table><thead><tr><th>COHORT</th><th>OBSERVATIONS</th><th>STILL OPEN</th><th>MEAN R</th><th>MEDIAN R</th><th>STOP HIT %</th></tr></thead><tbody>${s.rows.map(r=>`<tr><td>${esc(r.cohort)}</td><td>${number(r.n)}</td><td>${number(r.open)}</td><td>${signed(r.avg_r,2)}</td><td>${signed(r.med_r,2)}</td><td>${number(r.hit_stop_pct,1)}</td></tr>`).join('')}</tbody></table></div>`).join('')}</details>`;}
function pennyExclusions(){return `<details class="panel disclosure"><summary>What the penny gates excluded</summary><p class="small-note">Recorded exclusion reasons, preserved from the source. Surveillance status is an eligibility constraint, not proof of wrongdoing.</p>${(D.penny.funnel||[]).map(r=>`<div class="check-row"><span>${esc(r.why.replace(/\s*�\s*/g,' — '))}</span><b class="num">${number(r.n)}</b></div>`).join('')}</details>`;}
function companyFacts(r,penny){const rows=penny?[['Market cap',money(r.mcap)+' Cr'],['Median daily turnover',money(r.turn)+' Cr'],['Daily trades',number(r.trades)],['Circuit band',number(r.band)+'%'],['3-month price move',signed(r.run3m)+'%'],['Recorded risk flags',(r.flags||[]).join('; ')||'None recorded']]:[['Market cap',r.mcap==null?'—':money(r.mcap)+' Cr'],['ROCE',r.roce==null?'—':number(r.roce,1)+'%'],['P/E',number(r.pe,1)],['Trailing profit growth',r.pgttm==null?'—':signed(r.pgttm)+'%']];return `<h3>${penny?'Tradability & company facts':'Company fundamentals'}</h3>${rows.map(([k,v])=>`<div class="check-row"><span>${k}</span><b>${esc(v)}</b></div>`).join('')}`;}
let D, page='overview', chartPeriod=90, tablePage=1, stageFilter='', pennyFilter='eligible', newsFilter='all', drawerTab='thesis', selectedStock='', selectedPenny=false, researchMode='analyst', journalMode='signals', signalPage=1, query='', industry='', sortKey='rs', onlySaved=false;
let saved=new Set();try{saved=new Set(JSON.parse(localStorage.getItem('golden-stock-public-preview-watchlist')||'[]'));}catch{}
const $=s=>document.querySelector(s), esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const number=(v,n=0)=>v===null||v===undefined||!Number.isFinite(Number(v))?'—':Number(v).toLocaleString('en-IN',{maximumFractionDigits:n,minimumFractionDigits:n});
const money=v=>v==null?'—':'₹'+number(v,2), signed=(v,n=1)=>v==null?'—':(v>=0?'+':'')+number(v,n);
const names={overview:'Overview',screener:'Screener',news:'News radar',themes:'Themes',research:'AI research',penny:'Penny lab',portfolio:'Portfolio',journal:'Journal',evidence:'Evidence',system:'Data & system'};
const stageNames={CONFIRMED:'Uptrend',ANTICIPATION:'Basing',WATCH:'Neutral',EXTENDED:'Extended',BROKEN:'Downtrend'};
const dimNames={inflection:'Business inflection',momentum:'Price momentum',ownership:'Ownership',tradability:'Tradability',valuation:'Valuation',earnings_inflection:'Earnings inflection',rs_and_stage:'Relative strength & stage',theme_tailwind:'Theme tailwind',smart_money:'Ownership flows',financial_strength_trend:'Financial strength',catalyst:'Catalyst evidence',governance:'Governance',valuation_sanity:'Valuation sanity'};
const icons={overview:'<rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/><rect x="14" y="14" width="7" height="7"/>',screener:'<path d="M3 6h18M6 12h12M9 18h6"/><circle cx="8" cy="6" r="2"/><circle cx="16" cy="12" r="2"/>',news:'<circle cx="12" cy="12" r="9"/><circle cx="12" cy="12" r="5"/><path d="M12 12l7-7"/><circle cx="12" cy="12" r="1"/>',themes:'<path d="M4 20V10h4v10M10 20V4h4v16M16 20v-7h4v7"/>',research:'<path d="M12 3l2.5 6.5L21 12l-6.5 2.5L12 21l-2.5-6.5L3 12l6.5-2.5z"/>',penny:'<path d="M9 3h6M10 3v7L4 19q0 2 2 2h12q2 0 2-2l-6-9V3M7 15h10"/>',portfolio:'<rect x="3" y="7" width="18" height="14" rx="2"/><path d="M8 7V3h8v4M3 12h18M10 12v3h4v-3"/>',journal:'<rect x="5" y="3" width="15" height="18" rx="1"/><path d="M2 7h5M2 12h5M2 17h5M10 8h6M10 12h6M10 16h4"/>',evidence:'<path d="M12 2l8 4v6c0 5-8 10-8 10S4 17 4 12V6zM8 12l3 3 5-6"/>',system:'<circle cx="12" cy="12" r="8"/><path d="M12 8v5M12 16v1"/>'};
const icon=k=>`<svg viewBox="0 0 24 24" aria-hidden="true">${icons[k]||icons.overview}</svg>`;
const tag=(text,type='')=>`<span class="tag ${type}">${esc(text)}</span>`;
const stage=r=>tag(stageNames[r.tag]||r.tag||'Not assessed',r.tag==='BROKEN'?'red':r.tag==='EXTENDED'?'amber':r.tag==='WATCH'?'gray':'');
const stockButton=(sym,text)=>`<button class="stock-name" data-stock="${esc(sym)}">${esc(text||sym)}</button>`;
function head(title,sub,kicker='RESEARCH WORKSPACE'){return `<div class="page-heading"><div><span class="eyebrow">${kicker}</span><h1>${title}</h1><p>${sub}</p></div><div class="heading-note">NSE · INDIAN EQUITIES<br><b>Independent thinking. Systematic process.</b></div></div>`;}
function panelHead(title,sub='',link='',target=''){return `<div class="panel-head"><div><h2>${title}</h2>${sub?`<span class="eyebrow">${sub}</span>`:''}</div>${link?`<button class="text-button" data-page="${target}">${link} ↗</button>`:''}</div>`;}
function metric(label,value,foot){return `<div class="metric"><div class="metric-label">${label}<span>·</span></div><div class="metric-value num">${value}</div><div class="metric-foot">${foot}</div></div>`;}
function rowFor(sym){return D.rows.find(r=>r.sym===sym)||D.penny?.rows?.find(r=>r.sym===sym)||{sym,company:sym};}
function seriesFor(sym){return D.closes[sym]||D.penny?.closes?.[sym]||[];}
function lineChart(values,large=false){
 const a=values.map(v=>Number(Array.isArray(v)?v[1]:v)).filter(Number.isFinite);if(a.length<2)return '<div class="empty">No price series available in this snapshot.</div>';
 const w=large?600:90,h=large?155:26,min=Math.min(...a),max=Math.max(...a),pad=large?12:2,span=max-min||1;
 const pts=a.map((v,i)=>[pad+i*(w-pad*2)/(a.length-1),h-pad-(v-min)/span*(h-pad*2)]),p=pts.map(([x,y],i)=>`${i?'L':'M'}${x.toFixed(2)},${y.toFixed(2)}`).join(' ');
 return `<svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none" role="img" aria-label="${large?'Historical closing-price chart':'Historical price sparkline'}">${large?`<defs><linearGradient id="price-fill" x1="0" x2="0" y1="0" y2="1"><stop offset="0" stop-color="#c5b580" stop-opacity=".22"/><stop offset="1" stop-color="#c5b580" stop-opacity="0"/></linearGradient></defs>${[25,65,105,145].map(y=>`<line x1="0" x2="600" y1="${y}" y2="${y}" stroke="#77866c" stroke-opacity=".13" stroke-dasharray="3 5"/>`).join('')}<path d="${p} L${w-pad},${h} L${pad},${h} Z" fill="url(#price-fill)"/>`:''}<path d="${p}" fill="none" stroke="${large?'#cdbd89':a.at(-1)>=a[0]?'#72916a':'#b17c69'}" stroke-width="${large?1.8:1.5}" vector-effect="non-scaling-stroke"/>${large?`<circle cx="${pts.at(-1)[0]}" cy="${pts.at(-1)[1]}" r="3" fill="#d7c994"/>`:''}</svg>`;
}
function marketPanel(){const a=D.nifty.slice(-chartPeriod),last=a.at(-1)?.[1],change=a.length?100*(last/a[0][1]-1):null;return `<section class="panel dark-panel">${panelHead('Market pulse','NIFTY 50 · ARCHIVED CLOSES')}<div class="chart-top"><span class="chart-price num">${number(last,2)}</span><span class="chart-change ${change<0?'down':''}">${signed(change,2)}% in view</span><div class="periods" style="margin-left:auto">${[30,90,252].map(p=>`<button data-period="${p}" class="${p===chartPeriod?'selected':''}" aria-pressed="${p===chartPeriod}">${p===252?'1Y':p+'D'}</button>`).join('')}</div></div><div class="chart-wrap">${lineChart(a,true)}</div><div class="chart-bottom"><span>${esc(a[0]?.[0]||'')}</span><span>${chartPeriod} TRADING SESSIONS</span><span>${esc(a.at(-1)?.[0]||'')}</span></div><div class="breadth-row"><span>Breadth</span><strong>${number(D.breadth_pct,1)}%</strong><div class="mini-track"><i style="width:${D.breadth_pct}%"></i></div><span>above 200-DMA</span></div></section>`;}
function tableRows(rows,mode='normal'){return rows.map(r=>{const a=D.actionable.find(a=>a.sym===r.sym);return `<tr><td><div class="table-symbol"><span class="initial">${esc(r.sym.slice(0,2))}</span><div>${stockButton(r.sym)}<small>${esc(r.company||r.ind||'NSE equity')}</small>${mode!=='penny'&&r.veto?tag('Veto recorded','red'):mode!=='penny'&&r.cov<60?tag('Limited coverage','amber'):''}</div></div></td><td class="num">${money(r.close)}${mode==='focus'?`<small class="${(a?.chg||0)>=0?'positive':'negative'}">${a?.chg!=null?signed(a.chg)+'% since alert':'Archived close'}</small>`:''}</td><td>${mode==='penny'?tag(r.veto?'Vetoed':r.asd===false?'Incomplete':'Research',r.veto?'red':'amber'):stage(r)}</td><td class="num">${number(r.rs)}</td><td><div class="score-cell num">${number(r.score,1)}<i><b style="width:${Math.min(100,r.score||0)}%"></b></i></div></td>${mode==='normal'?`<td><span class="muted">${esc(r.trig||'No trigger recorded')}</span></td>`:''}${mode==='penny'?`<td class="num">${number(r.turn,2)} Cr</td>`:''}<td><button class="save-btn" data-save="${esc(r.sym)}" aria-label="${saved.has(r.sym)?'Remove':'Save'} ${esc(r.sym)} ${saved.has(r.sym)?'from':'to'} watchlist" aria-pressed="${saved.has(r.sym)}">${saved.has(r.sym)?'★':'☆'}</button></td></tr>`;}).join('');}
function stockTable(rows,mode='normal'){return `<div class="table-wrap"><table><thead><tr><th>COMPANY</th><th>LAST PRICE</th><th>${mode==='penny'?'REVIEW STATUS':'CHART STAGE'}</th><th>RS %</th><th>${mode==='penny'?'RESEARCH SCORE':'CONVICTION'}</th>${mode==='normal'?'<th>TECHNICAL TRIGGER</th>':''}${mode==='penny'?'<th>TURNOVER / DAY</th>':''}<th><span aria-label="Watchlist">☆</span></th></tr></thead><tbody>${tableRows(rows,mode)}</tbody></table>${!rows.length?'<div class="empty"><strong>No matches</strong>Try another company, stage or industry.</div>':''}</div>`;}
function themeCards(limit=100){return [...(D.themes.themes||[])].sort((a,b)=>(b.heat||0)-(a.heat||0)).slice(0,limit).map((t,i)=>`<article class="theme-card"><span class="theme-index">${String(i+1).padStart(2,'0')} / THEME</span><span class="theme-heat num">${number(t.heat)}<small style="font-size:9px"> /100</small></span><h3>${esc(t.name)}</h3><div class="theme-track"><b style="width:${t.heat||0}%"></b></div><div class="theme-meta"><span>${t.n} companies</span><span class="${t.ret3m>=0?'positive':'negative'}">${signed(t.ret3m)}% · 3 months</span></div><div class="theme-leaders">${(t.leaders||[]).slice(0,3).map(l=>`<button data-stock="${esc(l.sym)}">${esc(l.sym)} ↗</button>`).join('')}</div></article>`).join('');}
function memories(){return Object.entries(D.news_mem.syms||{}).map(([sym,n])=>({sym,...n})).sort((a,b)=>(b.rp||0)-(a.rp||0)||(b.p||0)-(a.p||0));}
function story(n){return `<article class="story"><button class="story-action" data-stock="${esc(n.sym)}"><div class="story-meta">${tag(n.n_neg?'Risk to review':'Developing thesis',n.n_neg?'red':'amber')}<span>${esc((n.last||'').slice(0,10))}</span></div><h3>${esc(n.sym)} <span class="muted">↗</span></h3><p>${esc(n.summary||'Archived event record')}</p></button></article>`;}
function overview(){const waiting=D.actionable.filter(a=>a.dokind==='watch'),rows=waiting.slice(0,5).map(a=>rowFor(a.sym));return `${head('The research desk.','A considered view of the market. A clear path to your next decision.','MARKET OVERVIEW / 01')}<div class="preview-strip"><span class="dot"></span><span>Design preview · Snapshot ${esc(D.generated)} · Equity prices ${esc(D.price_date)}</span><button class="strip-link" data-page="system">Data provenance ↗</button></div><div class="metrics">${metric('Universe coverage',number(D.rows.length), 'Equities in the saved scan')}${metric('Confirmed uptrends',number(D.tags.CONFIRMED),'<b>'+number(100*D.tags.CONFIRMED/D.rows.length,1)+'%</b> of the tracked universe')}${metric('Awaiting a trigger',waiting.length,'Research candidates · not buy orders')}${metric('Validation progress',D.gate.cohort.n_qualifying+'<span> / '+D.gate.required.min_signals+'</span>','Mature signals · still accruing')}</div><div class="grid-main">${marketPanel()}<section class="panel">${panelHead('The daily brief','A MOMENT TO TAKE STOCK')}<div class="brief-body"><span class="brief-badge">NO NEW BUY TRIGGERS</span><h3>Patience is<br>a position.</h3><p>The saved scan has no new entry triggers. Follow the developing setups; let the evidence catch up.</p><div class="brief-lines"><div class="brief-line"><span>Setups awaiting confirmation</span><strong>${waiting.length}</strong></div><div class="brief-line"><span>Capital gate</span><strong>Accruing evidence</strong></div></div><button class="btn full" data-page="screener">Explore the universe <span>↗</span></button></div></section></div><div class="grid-main lower"><section class="panel">${panelHead('On the horizon','BASES TO FOLLOW · NO CONFIRMED ENTRY','Open screener','screener')}${stockTable(rows,'focus')}<div class="small-note">Conviction is a research score, not the probability of a profitable trade.</div></section><section class="panel">${panelHead('Intelligence radar','EVIDENCE BEFORE OPINION','All news','news')}${memories().filter(n=>n.primed&&!n.n_neg).slice(0,3).map(story).join('')||'<div class="empty">No developing stories in this snapshot.</div>'}</section></div><div class="section-row"><h2>Where momentum is gathering</h2><button class="text-button" data-page="themes">Explore all themes ↗</button></div><div class="themes-grid">${themeCards(3)}</div>`;}
function screener(penny=false){const list=penny?D.penny.rows:D.rows;return `${head(penny?'Small companies. Bigger questions.':'Find your next research idea.',penny?'A separate laboratory for penny and nano-cap research. Tradability comes first.':'Explore chart structure, relative strength and supporting evidence.',penny?'PENNY LAB / RESEARCH ONLY':'THE UNIVERSE / 02')}${penny?'<div class="callout">Penny scores have not been validated as buy signals. “Assessed” means some survival inputs exist; it does not mean every risk was verified.</div>':''}<div class="toolbar"><input class="field" id="table-search" aria-label="Filter companies" placeholder="Search symbol or company…" value="${esc(query)}">${!penny?`<label>Industry<select class="field" id="industry"><option value="">All industries</option>${[...new Set(D.rows.map(r=>r.ind).filter(x=>x&&x!=='nan'))].sort().map(i=>`<option ${industry===i?'selected':''}>${esc(i)}</option>`).join('')}</select></label>`:''}<label>Sort by<select class="field" id="sort"><option value="rs" ${sortKey==='rs'?'selected':''}>Relative strength</option><option value="score" ${sortKey==='score'?'selected':''}>${penny?'Research score':'Conviction score'}</option><option value="sym" ${sortKey==='sym'?'selected':''}>Company A–Z</option><option value="turn" ${sortKey==='turn'?'selected':''}>Liquidity</option></select></label><button class="btn secondary" data-toggle-saved aria-pressed="${onlySaved}">${onlySaved?'★ Saved only':'☆ Watchlist'}</button></div><div class="filter-chips">${penny?['eligible','all','vetoed','incomplete'].map(v=>`<button class="chip ${pennyFilter===v?'active':''}" data-pennyfilter="${v}" aria-pressed="${pennyFilter===v}">${{eligible:'Assessed · no veto',all:'All research',vetoed:'Vetoed',incomplete:'Incomplete'}[v]}</button>`).join(''):['','CONFIRMED','ANTICIPATION','EXTENDED','WATCH','BROKEN'].map(v=>`<button class="chip ${stageFilter===v?'active':''}" data-stage="${v}" aria-pressed="${stageFilter===v}">${stageNames[v]||'All stages'}</button>`).join('')}</div><section class="panel"><div id="table-results"></div></section><div class="small-note" style="border:0;padding-left:0">${penny?`Archived penny screen: ${esc(D.penny.as_of)} · All records remain research only.`:'Relative strength ranks past price performance. A high rank or conviction score does not establish a buy trigger.'}</div>`;}
function updateTable(){let rows=[...(page==='penny'?D.penny.rows:D.rows)];if(query)rows=rows.filter(r=>(r.sym+' '+r.company).toLowerCase().includes(query.toLowerCase()));if(onlySaved)rows=rows.filter(r=>saved.has(r.sym));if(page==='penny'){if(pennyFilter==='eligible')rows=rows.filter(r=>!r.veto&&r.asd!==false);if(pennyFilter==='vetoed')rows=rows.filter(r=>r.veto);if(pennyFilter==='incomplete')rows=rows.filter(r=>r.asd===false);}else{if(stageFilter)rows=rows.filter(r=>r.tag===stageFilter);if(industry)rows=rows.filter(r=>r.ind===industry);}rows.sort((a,b)=>sortKey==='sym'?a.sym.localeCompare(b.sym):(b[sortKey]??-Infinity)-(a[sortKey]??-Infinity));let pages=Math.max(1,Math.ceil(rows.length/20));tablePage=Math.min(tablePage,pages);const start=(tablePage-1)*20;$('#table-results').innerHTML=stockTable(rows.slice(start,start+20),page==='penny'?'penny':'normal')+`<div class="pagination"><span>${rows.length?start+1:0}–${Math.min(start+20,rows.length)} of ${number(rows.length)} companies</span><div><button data-pagination="-1" ${tablePage===1?'disabled':''}>← Previous</button><button data-pagination="1" ${tablePage===pages?'disabled':''}>Next →</button></div></div>`;}
function news(){const rows=memories().filter(n=>newsFilter==='all'||(newsFilter==='risk'?n.n_neg>0:n.primed));return `${head('Read the event. See the context.','A company’s story, connected to its chart and your research.','INTELLIGENCE / 03')}<div class="callout">News directs attention and risk review. Positive coverage is not an automatic buy signal. Dates below belong to the archived snapshot.</div><div class="filter-chips">${[['all','All events'],['risk','Risk to review'],['building','Developing stories']].map(([k,v])=>`<button class="chip ${newsFilter===k?'active':''}" data-newsfilter="${k}" aria-pressed="${newsFilter===k}">${v}</button>`).join('')}</div><div class="memo-grid">${rows.slice(0,40).map(n=>`<section class="panel">${story(n)}<div class="small-note">${n.n_pos||0} positive · ${n.n_neg||0} negative · ${stageNames[rowFor(n.sym).tag]||'Chart not assessed'}<button class="text-button" style="float:right" data-stock="${esc(n.sym)}">Open company ↗</button></div></section>`).join('')||'<div class="empty">No matching archived stories.</div>'}</div><p class="small-note">Showing ${Math.min(40,rows.length)} of ${rows.length} company histories. Search any company to inspect its individual evidence.</p>`;}
function research(){if(researchMode==='committee')return committee();return `${head('A second perspective.','Company research from the analyst. Keep the thesis separate from the trigger.','AI RESEARCH / 05')}<div class="callout">Archived AI opinions, shown as originally recorded. These are separate from mechanical entry eligibility and are not current recommendations.</div>${researchTabs()}<div class="memo-grid">${D.verdict_items.map(v=>`<article class="panel memo"><div class="story-meta">${tag('AI '+v.verdict,v.verdict==='SKIP'?'red':v.verdict==='WAIT'?'amber':'')}<span>${esc(v.stamp||'Snapshot')}</span></div><h3>${stockButton(v.sym)}</h3><p>${esc(v.why)}</p><details><summary>Read the thesis & risks</summary><ul>${(v.whys||[]).map(t=>`<li>${esc(t)}</li>`).join('')}</ul><h4>Risks to examine</h4><ul>${(v.risks||[]).map(t=>`<li>${esc(t)}</li>`).join('')}</ul><p>${esc(v.flip||'')}</p></details><button class="text-button" data-stock="${esc(v.sym)}">Open the complete stock view ↗</button></article>`).join('')}</div>`;}
function researchTabs(){return `<div class="filter-chips"><button class="chip ${researchMode==='analyst'?'active':''}" data-research="analyst" aria-pressed="${researchMode==='analyst'}">Analyst memos</button><button class="chip ${researchMode==='committee'?'active':''}" data-research="committee" aria-pressed="${researchMode==='committee'}">Weekly committee</button></div>`;}
function committee(){const a=D.ai_picks||{};return `${head('The committee’s perspective.','A curated thesis for each company, with the reasons and risks kept visible.','AI RESEARCH / WEEKLY')}${researchTabs()}<div class="callout">Archived committee research · ${esc(a.generated||'Date unavailable')} · An AI selection is not a verified mechanical trigger.</div><section class="panel memo"><span class="eyebrow gold">PORTFOLIO VIEW</span><p style="margin-top:16px">${esc(a.portfolio_view||'No committee summary in this snapshot.')}</p><details><summary>Read the thematic perspective</summary><p>${esc(a.theme_read||'Not recorded')}</p></details></section><div class="section-row"><h2>Companies in focus</h2></div><div class="memo-grid">${(a.picks||[]).map(v=>`<article class="panel memo"><div class="story-meta">${tag('Committee thesis','amber')}<span>${esc(v.meta?.sector||'')}</span></div><h3>${stockButton(v.symbol)}</h3><p>${esc(v.thesis||v.selected_because||'')}</p><details><summary>Selection rationale, catalyst & risks</summary><p>${esc(v.selected_because||'')}</p><h4>Catalyst</h4><p>${esc(v.catalyst||'')}</p><h4>Risks</h4><p>${esc(v.risks||'')}</p><h4>What to watch</h4><p>${esc(v.watch_for||'')}</p></details><button class="text-button" data-stock="${esc(v.symbol)}">Inspect the company ↗</button></article>`).join('')}</div>`;}
function portfolio(){return `${head('Know what you own.','Position-level exposure and recorded plans, kept separate from paper results.','PORTFOLIO / 07')}<div class="callout">Personal holdings are excluded from this public preview. Paper results below are archived system simulations.</div><section class="panel">${panelHead('Recorded holdings','LOCAL POSITION SNAPSHOT')}<div class="table-wrap"><table><thead><tr><th>COMPANY</th><th>SHARES</th><th>ENTRY</th><th>LAST</th><th>RECORDED STOP</th><th>PRICE RETURN</th></tr></thead><tbody>${D.positions.map(r=>`<tr><td>${stockButton(r.sym)}</td><td>${number(r.shares)}</td><td>${money(r.entry)}</td><td>${money(r.last)}</td><td>${money(r.stop)}</td><td class="${r.pnl>=0?'positive':'negative'}">${signed(r.pnl,2)}%</td></tr>`).join('')}</tbody></table>${!D.positions.length?'<div class="empty"><strong>Your portfolio stays private.</strong>Personal holdings are not included in this public design preview.</div>':''}</div></section><div class="section-row"><h2>Paper portfolio</h2>${tag('Simulation','amber')}</div><div class="metrics financial-metrics">${metric('Recorded equity',money(D.paper.equity),'Original paper-book calculation')}${metric('Realized P&L',money(D.paper.realized),'Recorded estimate')}${metric('Unrealized P&L',money(D.paper.unrealized),'Marked at saved prices')}${metric('Closed positions',number(D.paper.n_closed),'Not a live broker account')}</div><div class="callout">Paper-book accounting and execution discrepancies were identified in the audit. Treat these figures as archived records awaiting reconciliation.</div>`;}
function journal(){if(journalMode==='signals')return signalJournal();return `${head('Every signal has a history.','An accountable record of what the system observed, and when.','JOURNAL / 08')}${journalTabs()}<div class="metrics">${metric('Journaled observations',number(D.journal_total),'Includes stage changes and research alerts')}${metric('Visible records',number(D.journal.length),'Most recent rows in the saved dashboard')}${metric('Mature gate observations',number(D.gate.cohort.n_qualifying),'A separate eligibility cohort')}${metric('Validation state','Accruing','No inference of deployment approval')}</div><section class="panel">${panelHead('Observation log','ARCHIVED EVENTS · MOST RECENT FIRST')}<div class="table-wrap"><table><thead><tr><th>OBSERVED AT</th><th>COMPANY</th><th>EVENT</th><th>BEFORE</th><th>AFTER</th></tr></thead><tbody>${D.journal.map(r=>`<tr><td class="muted">${esc(r.d)}</td><td>${stockButton(r.sym)}</td><td>${esc(r.kind)}</td><td>${esc(stageNames[r.old]||r.old||'—')}</td><td>${esc(stageNames[r.new]||r.new||'—')}</td></tr>`).join('')}</tbody></table></div></section>`;}
function evidence(){const c=D.gate.cohort;return `${head('Confidence needs a record.','Separate a promising hypothesis from evidence strong enough to rely on.','EVIDENCE / 09')}<div class="evidence-grid"><section class="panel"><span class="eyebrow gold">FORWARD VALIDATION</span><div class="metric-value" style="font-size:54px">${c.n_qualifying}<span> / ${D.gate.required.min_signals}</span></div><h2>Still building the sample.</h2><p>The archived gate is accruing mature observations. Sample size is a minimum condition, not proof of a profitable strategy.</p><div class="progress"><b style="width:${100*c.n_qualifying/D.gate.required.min_signals}%"></b></div><div class="check-row"><span>Mature-cohort mean R</span><b>${signed(c.expectancy_r,3)}R</b></div><div class="check-row"><span>Archived reference threshold</span><b>${signed(c.required_expectancy_r,3)}R</b></div><div class="check-row"><span>Record date</span><span>${esc(D.gate.generated)}</span></div></section><section class="panel"><span class="eyebrow gold">WHAT THE SCORES MEAN</span><h2 style="margin-top:22px">Three different questions.</h2><div class="check-row"><div><b>Technical trigger</b><p>Has the price-and-volume entry condition occurred?</p></div>${tag('Entry condition')}</div><div class="check-row"><div><b>Conviction</b><p>What supporting company evidence is available?</p></div>${tag('Heuristic','amber')}</div><div class="check-row"><div><b>News & penny research</b><p>What deserves investigation or risk review?</p></div>${tag('Unvalidated','gray')}</div><p style="margin-top:20px">A score of 80 is not an 80% chance of success. Coverage describes populated dimensions, not complete verification.</p></section></div><div class="section-row"><h2>Historical claims under review</h2></div><div class="callout">The original dashboard’s headline CAGR mixes gross equity with cost-adjusted trade metrics. The stress figure represents a different configuration. Both remain in the original view for comparison; neither is presented here as a verified return forecast.</div>`;}
function system(){return `${head('The provenance behind the screen.','Freshness, coverage and the boundaries of this preview.','DATA & SYSTEM')}<div class="callout">This is a published design prototype. It reads one sanitized archived dashboard snapshot and cannot run scans, change scoring weights, modify positions or send alerts.</div><section class="panel">${panelHead('Snapshot manifest','EXPLICIT DATES · NO LIVE STATUS CLAIM')}<div class="system-list"><div class="check-row"><span>Dashboard generated</span><b>${esc(D.generated)}</b></div><div class="check-row"><span>Equity price date</span><b>${esc(D.price_date)}</b></div><div class="check-row"><span>Last recorded scan</span><b>${esc(D.scan_date)}</b></div><div class="check-row"><span>Universe / saved details</span><b>${number(D.rows.length)} / ${number(Object.keys(D.details).length)}</b></div>${D.health.map(h=>`<div class="check-row"><div><b>${esc(h.label||h.k)}</b><p>${esc(h.detail||'')}</p></div><span>${esc(h.at||'Timestamp unavailable')}</span></div>`).join('')}</div></section><div class="section-row"><h2>Preview preferences</h2></div><button class="btn secondary" id="clear-saved">Clear preview watchlist (${saved.size})</button><p class="small-note" style="border:0">Only the watchlist in this browser’s design preview is affected. The production dashboard is unaffected.</p>`;}
function render(){document.title=`${names[page]} · Golden Stock — Design ${DESIGN}`;$('#crumb').textContent=names[page];$('#nav').innerHTML=`<div class="nav-label">DISCOVER</div>${['overview','screener','news','themes','research','penny'].map(navButton).join('')}<div class="nav-label">MEASURE</div>${['portfolio','journal','evidence','system'].map(navButton).join('')}`;const views={overview,screener:()=>screener(false),penny:()=>screener(true),news,research,portfolio,journal,evidence,system,themes:()=>`${head('Follow the structural shifts.','Industry boundaries are narrow. Investment themes connect the bigger picture.','THEMES / 04')}<div class="callout">Theme heat is a relative research ranking built from price, breadth and news. It is not an independent prediction or a buy instruction.</div><div class="themes-grid">${themeCards()}</div>${industryReview()}`};$('#workspace').innerHTML=`<div class="page-enter">${views[page]()}${page==='evidence'?cohortReview():''}${page==='penny'?pennyExclusions():''}</div>`;if(page==='screener'||page==='penny')updateTable();}
function navButton(k){return `<button class="nav-link ${page===k?'active':''}" data-page="${k}" title="${names[k]}" aria-label="${names[k]}" ${page===k?'aria-current="page"':''}>${icon(k)}<span>${names[k]}</span>${k==='penny'?'<span class="nav-tail">LAB</span>':''}</button>`;}
function navigate(k){if(!names[k])return;page=k;query='';industry='';tablePage=1;$('#sidebar').classList.remove('open');$('#menu').setAttribute('aria-expanded','false');location.hash=k;render();window.scrollTo({top:0,behavior:'instant'});}
function showToast(t){$('#toast').textContent=t;$('#toast').classList.add('on');clearTimeout(showToast.timer);showToast.timer=setTimeout(()=>$('#toast').classList.remove('on'),2300);}
function save(sym){if(saved.has(sym))saved.delete(sym);else saved.add(sym);try{localStorage.setItem('golden-stock-public-preview-watchlist',JSON.stringify([...saved]));}catch{}showToast(saved.has(sym)?sym+' saved to your preview watchlist':sym+' removed from your preview watchlist');if(page==='screener'||page==='penny')updateTable();else if(!$('#stock-dialog').open)render();if($('#stock-dialog').open)drawStock();}
function candles(sym){if(selectedPenny)return lineChart((D.penny.closes[sym]||[]).slice(-90),true);const rows=(D.ohlc[sym]||[]).slice(-55);if(rows.length<2)return lineChart(seriesFor(sym).slice(-90),true);const vals=rows.flatMap(r=>[r[2],r[3]]),lo=Math.min(...vals),hi=Math.max(...vals),range=hi-lo||1,y=v=>180-(v-lo)/range*160;return `<svg viewBox="0 0 600 190" role="img" aria-label="${esc(sym)} historical daily candlesticks">${[20,60,100,140,180].map(v=>`<line x1="0" x2="600" y1="${v}" y2="${v}" stroke="#6b8066" stroke-opacity=".2"/>`).join('')}${rows.map((r,i)=>{let x=9+i*580/rows.length,c=r[4]>=r[1]?'#b6c397':'#bf8e7b';return `<line x1="${x}" x2="${x}" y1="${y(r[2])}" y2="${y(r[3])}" stroke="${c}"/><rect x="${x-2.7}" width="5.4" y="${Math.min(y(r[1]),y(r[4]))}" height="${Math.max(1,Math.abs(y(r[1])-y(r[4])))}" fill="${c}"/>`;}).join('')}</svg>`;}
function openStock(sym){selectedStock=sym;selectedPenny=(page==='penny'&&D.penny.rows.some(r=>r.sym===sym))||!D.rows.some(r=>r.sym===sym);drawerTab='thesis';drawStock();if($('#search-dialog').open)$('#search-dialog').close();if(!$('#stock-dialog').open)$('#stock-dialog').showModal();}
function drawStock(){const r=(selectedPenny?D.penny.rows.find(r=>r.sym===selectedStock):null)||rowFor(selectedStock),penny=selectedPenny,detail=(penny?D.penny?.details?.[selectedStock]:D.details[selectedStock])||{},news=detail.news||{},v=D.verdict_items.find(v=>v.sym===selectedStock),mem=D.news_mem.syms[selectedStock],plan=detail.plan||{};const dims=detail.dims||detail.blocks||[];
let body='';
if(drawerTab==='thesis')body=`<div class="callout">${r.veto?'A veto is recorded. Review the reason before considering this company.':r.asd===false?'Survival inputs are incomplete. The absence of a veto is not a clean bill of health.':detail.vcp?'A base is identified. A base alone does not establish a breakout entry.':'Research context only. Confirm a fresh technical trigger and current data before acting.'}</div><div class="fact-grid"><div class="fact">Relative strength percentile<b>${number(r.rs)}<small style="font-size:10px"> /100</small></b></div><div class="fact">${penny?'Penny research score':'Conviction score'}<b>${number(r.score??detail.score,1)}<small style="font-size:10px"> /100</small></b></div></div><h3>The technical read</h3><p>${esc((detail.reasons||[]).join('. ')||r.trig||'No detailed technical explanation stored in this snapshot.')}</p><h3>Company context</h3><p>${esc(v?.why||r.arch||'No analyst thesis is stored for this company in the selected snapshot.')}</p>${mem?`<h3>What has been developing</h3><p>${esc(mem.summary)}</p>`:''}${(detail.veto_reasons||[]).length?`<h3>Risk flags</h3><p>${esc(detail.veto_reasons.join('; '))}</p>`:''}${r.vetowhy?`<h3>Penny veto</h3><p>${esc(r.vetowhy)}</p>`:''}${companyFacts(r,penny)}<h3>Evidence coverage</h3><p>${number(r.cov??detail.coverage)}% of weighted dimensions populated. This does not establish that all governance or survival checks are verified.</p>`;
if(drawerTab==='technical')body=`<div class="panel dark-panel"><div class="chart-wrap">${candles(selectedStock)}</div><div class="small-note" style="border:0;color:#9aaa92">Archived daily prices · ${esc(penny?D.penny.as_of:D.price_date)} · No live quote</div></div><h3>Price structure</h3><div class="fact-grid"><div class="fact">Recorded pivot<b>${money(detail.pivot_price)}</b></div><div class="fact">Trend checks passed<b>${detail.tt_checks==null?'—':detail.tt_checks+' / 8'}</b></div></div><h3>Archived plan</h3><p>Recorded levels explain the original plan. They are not executable prices for today.</p><div class="check-row"><span>Recorded entry</span><b>${money(plan.entry_price)}</b></div><div class="check-row"><span>Recorded initial stop</span><b>${money(penny?detail.stop:plan.stop_loss_price)}</b></div><div class="check-row"><span>Position sizing excluded from public preview</span><b>${number(plan.shares_total)}</b></div>`;
if(drawerTab==='score')body=`<div class="callout">These are heuristic weights, not calibrated probabilities. Some underlying inputs are missing even when a dimension is populated. Archived diagnostics can differ in age from the table.</div>${detail.scored_at?`<p>Score recorded ${esc(detail.scored_at)}</p>`:''}${dims.length?dims.map(d=>`<div class="dim-row"><div class="dim-row-top"><strong>${esc(dimNames[d.k]||d.k)}</strong><span>${d.s==null?'Missing':number(d.s*100)} <span class="muted">· weight ${d.w}%</span></span></div><div class="dim-bar"><span style="width:${Math.max(0,Math.min(100,(d.s||0)*100))}%"></span></div><p>${esc(d.n)}</p></div>`).join(''):`<div class="empty"><strong>No dimension breakdown stored</strong>${penny?'Penny weights: inflection 30%, momentum 25%, ownership 20%, tradability 15%, valuation 10%.':'The archived snapshot has no detailed score breakdown for this company.'}</div>`}`;
if(drawerTab==='news'){const stories=[...(news.filings||[]).map(n=>({d:n.d,t:n.t,s:'Exchange filing',u:n.u})),...(news.headlines||[]).map(n=>({d:n.d,t:n.t,s:n.s,u:n.u})),...(D.archive_news[selectedStock]||[]).map(n=>({d:n.d,t:n.t,s:n.src,u:n.u}))];body=`<div class="callout">Review the event’s materiality and original source. Headline sentiment does not establish future returns.</div>${mem?`<h3>90-day event memory</h3><p>${esc(mem.summary)}</p>`:''}${stories.length?stories.slice(0,12).map(n=>`<article class="story" style="padding-left:0;padding-right:0"><div class="story-meta">${esc(n.s||'Archived source')} · ${esc(n.d||'Date unavailable')}</div><h3>${esc(n.t)}</h3>${/^https?:\/\//i.test(n.u||'')?`<a class="text-button note-link" target="_blank" rel="noopener noreferrer" href="${esc(n.u)}">Read original source ↗</a>`:''}</article>`).join(''):'<div class="empty">No headline text stored for this company.</div>'}`;}
$('#stock-content').innerHTML=`<div class="drawer-header"><div class="drawer-top"><span class="eyebrow gold">COMPANY DOSSIER · NSE</span><button class="icon-btn" id="close-stock" aria-label="Close company details">×</button></div><h2 id="stock-title">${esc(r.sym)}</h2><p>${esc(r.company||'NSE-listed equity')} · ${esc(penny?'Penny & nano-cap research':r.ind||'Research universe')}</p><div class="drawer-price"><span>${money(r.close)}</span><button class="btn secondary" data-save="${esc(r.sym)}">${saved.has(r.sym)?'★ Saved':'☆ Save to watchlist'}</button></div><div style="margin-top:12px">${stage(r)} ${tag('Prices · '+(penny?D.penny.as_of:D.price_date),'gray')}</div></div><div class="drawer-tabs">${['thesis','technical','score','news'].map(k=>`<button class="${drawerTab===k?'active':''}" data-drawer="${k}" aria-pressed="${drawerTab===k}">${{thesis:'Thesis',technical:'Price & plan',score:'Score anatomy',news:'News & filings'}[k]}</button>`).join('')}</div><div class="drawer-content">${body}</div>`;
}
function searchResults(){const q=$('#global-search').value.toLowerCase().trim(),all=[...D.rows,...D.penny.rows.filter(p=>!D.rows.some(r=>r.sym===p.sym))],rs=all.filter(r=>!q||(r.sym+' '+r.company).toLowerCase().includes(q)).slice(0,8);$('#search-results').innerHTML=rs.length?rs.map(r=>`<button class="search-result" data-stock="${esc(r.sym)}"><span class="initial">${esc(r.sym.slice(0,2))}</span><span><b>${esc(r.sym)}</b><small>${esc(r.company||'NSE equity')}</small></span>${stage(r)}</button>`).join(''):'<div class="empty">No company matches that search.</div>';}
function openSearch(){$('#search-dialog').showModal();$('#global-search').value='';searchResults();$('#global-search').focus();}
document.addEventListener('click',e=>{const b=e.target.closest('button');if(!b||!D)return;if(b.dataset.page){navigate(b.dataset.page);return;}if(b.dataset.stock){openStock(b.dataset.stock);return;}if(b.dataset.save){save(b.dataset.save);return;}if(b.dataset.period){chartPeriod=Number(b.dataset.period);render();return;}if(b.hasAttribute('data-stage')){stageFilter=b.dataset.stage;tablePage=1;render();return;}if(b.dataset.pennyfilter){pennyFilter=b.dataset.pennyfilter;tablePage=1;render();return;}if(b.dataset.newsfilter){newsFilter=b.dataset.newsfilter;render();return;}if(b.dataset.pagination){tablePage+=Number(b.dataset.pagination);updateTable();return;}if(b.hasAttribute('data-toggle-saved')){onlySaved=!onlySaved;tablePage=1;render();return;}if(b.dataset.journal){journalMode=b.dataset.journal;render();return;}if(b.dataset.signalpage){signalPage+=Number(b.dataset.signalpage);render();return;}if(b.dataset.research){researchMode=b.dataset.research;render();return;}if(b.dataset.drawer){drawerTab=b.dataset.drawer;drawStock();return;}if(b.id==='search-open')openSearch();if(b.id==='close-menu'){$('#sidebar').classList.remove('open');$('#menu').setAttribute('aria-expanded','false');$('#menu').focus();}if(b.id==='close-search')$('#search-dialog').close();if(b.id==='close-stock')$('#stock-dialog').close();if(b.id==='menu'){const opened=$('#sidebar').classList.toggle('open');b.setAttribute('aria-expanded',String(opened));}if(b.id==='clear-saved'){saved.clear();try{localStorage.removeItem('golden-stock-public-preview-watchlist');}catch{}render();showToast('Preview watchlist cleared');}});
document.addEventListener('input',e=>{if(e.target.id==='table-search'){query=e.target.value;tablePage=1;updateTable();}if(e.target.id==='global-search')searchResults();});
document.addEventListener('change',e=>{if(e.target.id==='industry'){industry=e.target.value;tablePage=1;updateTable();}if(e.target.id==='sort'){sortKey=e.target.value;tablePage=1;updateTable();}});
document.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();if(D)openSearch();}if(e.key==='Escape'){$('#sidebar').classList.remove('open');$('#menu').setAttribute('aria-expanded','false');}});
for(const d of document.querySelectorAll('dialog'))d.addEventListener('click',e=>{if(e.target===d){const r=d.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)d.close();}});
window.addEventListener('hashchange',()=>{const k=location.hash.slice(1);if(names[k]&&page!==k){page=k;render();window.scrollTo(0,0);}});
fetch('../shared/snapshot.json').then(r=>{if(!r.ok)throw Error('Snapshot could not be loaded');return r.json();}).then(data=>{D=data;page=names[location.hash.slice(1)]?location.hash.slice(1):'overview';render();}).catch(err=>{$('#workspace').innerHTML=`<div class="empty"><strong>Unable to open the snapshot</strong>${esc(err.message)}<br>Open this preview through its local server.</div>`;});
