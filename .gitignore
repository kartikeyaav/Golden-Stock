# secrets — never commit
telegram_config.json
.env

# regenerable data caches (rebuilt by fetch scripts)
multibagger_screener/multibagger_screener/data_cache/
multibagger_screener/multibagger_screener/fundamentals_cache/
# raw NSE downloads for the penny screen (bhavcopy stack, symbol master,
# band/GSM/ASM lists) — all re-fetchable in ~1 min
multibagger_screener/multibagger_screener/nse_cache/

# operational noise
multibagger_screener/multibagger_screener/logs/
multibagger_screener/multibagger_screener/state/
multibagger_screener/multibagger_screener/matrix_trades/
*.log
__pycache__/
*.pyc

# office temp files
~$*.docx
*.docx

# test artifacts
multibagger_screener/multibagger_screener/test_alert.md

# built artifact — Pages rebuilds it fresh every publish; the daily-run
# artifact carries a downloadable copy (untracked 2026-07-20 audit; it was
# committed once historically and then never updated by the cloud = stale)
multibagger_screener/multibagger_screener/dashboard.html
multibagger_screener/multibagger_screener/_site/

# UI captures are regenerable from scripts/shoot_ui.py
multibagger_screener/multibagger_screener/ui_shots/

# personal financial data — the user's real book. Removed from the repo
# 2026-08-04 at their request (audit finding F5, open since 2026-07-25).
# Kept on disk so position_manager still works locally; never committed, so
# the public repo and the published Pages site stop carrying them.
# NOTE: prior commits still contain them — see HANDOFF §5 item 7.
multibagger_screener/multibagger_screener/positions.csv
multibagger_screener/multibagger_screener/holdings.csv
